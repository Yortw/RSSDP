<Page
    x:Class="RssdpUwpPackageTests.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RssdpUwpPackageTests"
		xmlns:rssdp="using:Rssdp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<ListView x:Name="DevicesList" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="rssdp:DiscoveredSsdpDevice">
					<StackPanel Margin="10" Orientation="Vertical">
						<TextBlock Text="{x:Bind Usn}" FontWeight="Bold" />
						<TextBlock Text="{x:Bind DescriptionLocation.AbsoluteUri}" />
					</StackPanel>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</Page>
